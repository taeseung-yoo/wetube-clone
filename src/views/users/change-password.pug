extends ../base
block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="oldPassword", type="password", required, placeholder="Old Password")
        input(name="newPassword", type="password", required, placeholder="New Password")
        input(name="newPassword2", type="password", required, placeholder="New Password Confirmation")
        input(type="submit", value="Change Password") 